/* Include guards */
#ifndef SUPPORT_H_INCLUDED
#define SUPPORT_H_INCLUDED

/* Prototype of functions support */
double maximo(const double arr[], int len);
void printArray(const double arr[], int len);
double amplitude(double arr[], int len);
double sort(double arr[], int len);
int fatorial(int n);
int combinacao(int n, int k);

#endif // SUPPORT_H_INCLUDED
